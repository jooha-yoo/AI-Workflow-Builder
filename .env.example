DATABASE_URL="file:./dev.db"

# Required — get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional — get a free key at https://tavily.com/. Without it, the web_search
# tool falls back to a clearly-labeled mock result so the app still runs fully locally.
TAVILY_API_KEY=
